<%= render 'resources/assets/form_header' %>
<%= form_tag process_vehicle_step_five_path do %>
  <b>Related Expenses</b>
  <br>
  <br>
  <%= fields_for :gasoline, @gasoline do |g| %>
    <%= g.label :amount, "How much do you typically spend on gas in a month?" %>
    <br>
    <%= form_amount(g) %>
    <%= g.label :associated_asset_id, "paid using: " %>
    <%= form_select_primary_asset(g, @user) %>
  <% end %>
  <br>
  <br> 
  <%= fields_for :insurance, @insurance do |i| %>
    <%= i.label :amount, "How much do you pay for car insurance in a year?" %>
    <br>
    <%= form_amount(i) %>
    <%= i.label :associated_asset_id, "paid using: " %>
    <%= form_select_primary_asset(i, @user) %>
  <% end %>
  <br>
  <br> 
  <%= fields_for :vehicle_maintenance, @vehicle_maintenance do |m| %>
    <%= m.label :amount, "How much do you typically spend on maintenance and repairs in a year?" %>
    <br>
    <%= form_amount(m) %>
    <%= m.label :associated_asset_id, "paid using: " %>
    <%= form_select_primary_asset(m, @user) %>
  <% end %>
  <br>
  <br> 
  <%= fields_for :miscellaneous, @miscellaneous do |mi| %>
    <%= mi.label :amount, "How much do you typically spend on miscellaneous expenses in a year?" %>
    <br>
    <%= form_amount(mi) %>
    <%= mi.label :associated_asset_id, "paid using: " %>
    <%= form_select_primary_asset(mi, @user) %>
  <% end %>
  <br>
  <br>
  <%= submit_tag "Submit" %>
  
<% end %>